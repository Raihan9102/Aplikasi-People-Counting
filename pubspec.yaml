name: project_ta
description: Real-Time People Detection and Monitoring
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^1.1.0
  google_sign_in: ^6.2.1
  firebase_core: ^4.6.0
  firebase_auth: ^6.4.0
  firebase_database: ^12.2.0
  cloud_firestore: ^6.3.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  camera: ^0.11.2+1
  permission_handler: ^12.0.1
  easy_localization: ^3.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
